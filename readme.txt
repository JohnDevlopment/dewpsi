README
======

Copy the following code into your terminal (each line starts with a '%' -- do not copy that!). If reading this in a text editor, do not copy the \` symbols either.

- - -

Shell:

    % export LD_LIBRARY_PATH
    % LD_LIBRARY_PATH=/home/john/programming/C++/projects/dewpsi/build

